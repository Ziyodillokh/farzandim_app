// Basic smoke test placeholder.
//
// Real testlar Bosqich 8.B keyin qo'shiladi (Firebase emulator + mock
// providerlar bilan). Hozircha `flutter analyze` toza qolishi uchun
// minimal test.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, equals(2));
  });
}
